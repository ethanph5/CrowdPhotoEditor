<!DOCTYPE html>
<html>
<body>
  <div class="container">
    <div class="header">
      <h2 id="header-icon"></h2>
      <!--get the current username from user login info or database-->

      <h4 id="username_text" >Hello, <%= @user_name%></h4>
      <%= link_to "Log Out", :controller => :account, :action => :logout %> 
      <!-- <form name="logoutPage" action="/">
        <input class="logout" class="logout buttom" id="logout-text" type="submit" value="Logout" />
      </form> -->
    </div>

    <div class="bottom">
      <h3 id="selectPhoto">0 Photo(s) Selected</h3><br/>
      <h2 id="myAlbum">My Album:</h2>
      <div class="facebookAlbum">
      	<% if @albums.nil?%>
      	<p>Welcome to Crowd Photo Editor</p>
      	<% else%>
      	<p>Facebook Albums</p>
      	<% @albums.each do |album|%>
      	<table class ="ablumTable">
      	<!-- <%= link_to album.name, album.link%><br/> -->
      		<tr>
      		<td><a href=<%= album.link%>>
      		<img src=<%= album.picture%> alt=<%= album.name%> width="100" height="100" border="0" />
      		</a> </td>
      		</tr>
      		<tr>
      		<td class="albumName"><%= album.name%> </td>
      		</tr>
      		<tr>
      		<td class="albumName"><%= album.count%> photos</td>
      		</tr>
      
      </table>
      <%end%>
      <%end%>
      
      <div class="localAlbum">
      <% if @albumList != nil %>
        <ul>
          <% @albumList.each do |album| %>
          <li type=disc id="album1"><%= link_to "#{album.name}", :action => 'selectPhoto', :id => album.id %></li>
          <% end %>
        </ul>
      <% else %>
        <h2 id="myAlbum">You have 0 album.</h2>
      <% end %>
      </div>
      
      
     </div>
     
	<div class="uoload action">

	  <form name="upload" action="/dashboard/uploadPhotoToNew">
		Upload new photos:
		<input type="submit" value="Upload" width="70px"/>
	  </form>
	</div>

    </div>
  </div>
</body>
</html>
