<div class= "header">
	<div id= "header-logo">
		<%= link_to image_tag('logo.png'), :controller => :account, :action=>:welcome %>
	</div>
	<div class= "logout">	
    	<!--<%= link_to "Log Out", :controller => :account, :action => :logout %>-->
    	<%= button_to "Log Out", {:controller => :account, :action => :logout}, {:class => "logout-button"} %>
    </div>
</div> 
    
<div class = "body">
    <h3 id="greet" >Hello, <%= @user_name%></h3>
    <div class= "picture-select">
      <h4>Your Picture:</h4>
      <BLOCKQUOTE>
      <% if @selected_picture == {} %>
        <h5>0 Picture(s) selected</h5>
      <% else %>
        <h5><%= @pictureSelected.length %> Picture(s) selected</h5>
        <% @pictureSelected.each do |pic| %>
          <li><%= pic.name %></li>
        <% end %>  
      <% end %>
      <BLOCKQUOTE>
    </div>
    <div class= "albums-list">
    	<h4 id="myAlbum">Facebook Album:</h4>
    	
    	<div class="facebook-photos">
      	<% if @albums.nil?%>
      		<p>Welcome to Crowd Photo Editor</p>
      	<% else%>
      	<% @albums.each do |album|%>
      	<div class = "facebook-album">
      		<a href=<%= album.link%>>
      		<img src=<%= album.picture%> alt=<%= album.name%> width="150" height="120" border="0" />
      		</a>
      		<p><%= link_to album.name, album.link,  :class => "album-name"%></p>
      		<p><%= album.count%> Photos</p><br/>  		
      	</div>
      	
      <%end%>
      <%end%>
      </div> 
  <br/><br/><br/><br/><br/><br/><br/>
  <h4>Crowd Album:</h4>
  <% Album.all.each do |item|%>
    <li><%= link_to item.name, :action => "selectPhoto" , :album_id => item.id %></li>
  <% end %>
  </div>
</div>
<div class="foot">
	<div class="upload-action">
		  <form name="upload" action="/dashboard/uploadPhotoToNew">
			Upload new photos:
			<input type="submit" value="Upload" width="70px"/>
		  </form>
	</div>
	<div class="doneSelection">
	    <form name="done" action="/dashboard/specifyTask">
	      <input type="submit" value="Done Selection" width="70px"/>
	    </form>
	</div>
</div>



<!--
<!DOCTYPE html>
<html>
<body>
  <div class="container">
    <div class="header">
      <h2 id="header-icon"></h2>

      <h4 id="username_text" >Hello, <%= @user_name%></h4>
      <%= link_to "Log Out", :controller => :account, :action => :logout %> 
    </div>

    <div class="bottom">
      	
      <br/>
      <h2 id="myAlbum">My Album:</h2>
      <div class="facebookAlbum">
      	<% if @albums.nil?%>
      	<p>Welcome to Crowd Photo Editor</p>
      	<% else%>
      	<p>Facebook Albums</p>
      	<% @albums.each do |album|%>
      	<table class ="ablumTable">
      	
      		<tr>
      		<td><a href=<%= album.link%>>
      		<img src=<%= album.picture%> alt=<%= album.name%> width="100" height="100" border="0" />
      		</a> </td>
      		</tr>
      		<tr>
      		<td class="albumName"><%= album.name%> </td>
      		</tr>
      		<tr>
      		<td class="albumName"><%= album.count%> photos</td>
      		</tr>
      
      </table>
      <%end%>
      <%end%>
      
      <hr />
      <div class="localAlbum">
      <% if @crowdAlbums.length != 0 %>
      	<h2 id="crowdAlbum">You have the following crowd album(s).</h2>
        <ul>
          <% @crowdAlbums.each do |album| %>
          <li type=disc id="album1"><%= link_to "#{album.name}", :action => 'selectPhoto', :album_id => album.id %></li>
          <% end %>
        </ul>
      <% else %>
        <h2 id="myAlbum">You have 0 crowd album.</h2>
      <% end %>
      </div>
      
      
     </div>
     
	<div class="uoload action">

	  <form name="upload" action="/dashboard/uploadPhotoToNew">
		Upload new photos:
		<input type="submit" value="Upload" width="70px"/>
	  </form>
	</div>

    </div>
  </div>
</body>
</html>
-->
